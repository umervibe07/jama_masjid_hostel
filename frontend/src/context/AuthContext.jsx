import {createContext,useContext,useEffect,useState} from 'react';import {api} from '@/lib/api';const C=createContext(null);export function AuthProvider({children}){const[user,setUser]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{const t=localStorage.getItem('masjid_admin_token');if(!t)return setLoading(false);api.get('/auth/me').then(r=>setUser(r.data)).catch(()=>localStorage.removeItem('masjid_admin_token')).finally(()=>setLoading(false))},[]);const login=async(email,password)=>{const{data}=await api.post('/auth/login',{email,password});localStorage.setItem('masjid_admin_token',data.token);setUser(data.user);return data.user};const logout=()=>{localStorage.removeItem('masjid_admin_token');setUser(null)};return <C.Provider value={{user,loading,login,logout}}>{children}</C.Provider>}export const useAuth=()=>useContext(C);
