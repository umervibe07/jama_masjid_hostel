import axios from 'axios';const base=process.env.REACT_APP_BACKEND_URL||'http://localhost:8001';export const api=axios.create({baseURL:`${base}/api`});api.interceptors.request.use(c=>{const t=localStorage.getItem('masjid_admin_token');if(t)c.headers.Authorization=`Bearer ${t}`;return c});export const formatError=e=>{const d=e?.response?.data?.detail;return typeof d==='string'?d:Array.isArray(d)?d.map(x=>x.msg).join(', '):e?.message||'Something went wrong'};
